services:
  - type: web
    name: deepfake-detection-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --workers 1 --threads 1 backend_api:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"